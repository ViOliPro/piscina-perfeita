# ============================================================
#  Piscina Perfeita — variáveis de ambiente
#  Copie este arquivo para .env e ajuste os valores.
# ============================================================

# URL base da API ASP.NET Core
# Em desenvolvimento o Vite proxy já redireciona /api → localhost:5000,
# então esta variável só é necessária em produção.
VITE_API_BASE_URL=http://localhost:5000/api
